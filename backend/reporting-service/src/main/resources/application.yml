server:
  port: 8084

spring:
  application:
    name: reporting-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,circuitbreakers

logging:
  level:
    com.caringiggy: DEBUG
